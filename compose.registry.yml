services:
  backend:
    image: ${BACKEND_IMAGE:-ghcr.io/dennis0405/ego-flow-server-backend:${IMAGE_TAG:-latest}}
    pull_policy: always

  worker:
    image: ${BACKEND_IMAGE:-ghcr.io/dennis0405/ego-flow-server-backend:${IMAGE_TAG:-latest}}
    pull_policy: always

  dashboard:
    image: ${DASHBOARD_IMAGE:-ghcr.io/dennis0405/ego-flow-server-dashboard:${IMAGE_TAG:-latest}}
    pull_policy: always
